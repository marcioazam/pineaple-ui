/**
 * Pineapple UI Design Tokens for Tailwind CSS
 * Single source of truth - imports from @pineapple-ui/tokens
 * 
 * Uses oklch color space for perceptually uniform colors
 * Spacing based on 4px base unit
 */
import { tailwindTheme } from '@pineapple-ui/tokens';

export const theme = tailwindTheme;
