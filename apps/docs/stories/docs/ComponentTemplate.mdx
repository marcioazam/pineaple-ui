{/* ComponentTemplate.mdx - Template for component documentation */}
import { Meta, Canvas, Controls, Story, Source, ArgTypes } from '@storybook/blocks';

{/* 
  Use this template as a guide for documenting components.
  Copy and adapt for each component's documentation page.
*/}

# Component Name

Brief description of what the component does and when to use it.

## Import

```tsx
import { ComponentName } from '@pineapple-ui/core';
```

## Usage

Basic usage example with code.

## Props

Use `<ArgTypes of={ComponentName} />` to auto-generate props table.

## Variants

Show different visual variants of the component.

## Sizes

Show different size options if applicable.

## States

Show different states (disabled, loading, error, etc.).

## Keyboard Interactions

| Key | Description |
|-----|-------------|
| `Tab` | Moves focus to the component |
| `Enter` | Activates the component |
| `Space` | Activates the component |
| `Escape` | Closes/dismisses if applicable |

## Accessibility

### ARIA Attributes

| Attribute | Value | Description |
|-----------|-------|-------------|
| `role` | `button` | Identifies the element as a button |
| `aria-disabled` | `true/false` | Indicates disabled state |
| `aria-pressed` | `true/false` | For toggle buttons |

### Best Practices

- Always provide accessible labels
- Ensure sufficient color contrast
- Support keyboard navigation
- Test with screen readers

## Examples

### Example 1: Basic Usage

Description of the example.

### Example 2: With Custom Styling

Description of the example.

## Related Components

- [RelatedComponent1](/docs/components-relatedcomponent1--docs)
- [RelatedComponent2](/docs/components-relatedcomponent2--docs)
