import * as React from 'react';
import { cn } from '@pineapple-ui/utils';
import { {{camelCase name}}Variants, type {{pascalCase name}}Variants } from './{{kebabCase name}}.styles';

export interface {{pascalCase name}}Props
  extends React.HTMLAttributes<HTMLDivElement>,
    {{pascalCase name}}Variants {
  /** Additional CSS classes */
  className?: string;
}

/**
 * {{pascalCase name}} component.
 *
 * @example
 * ```tsx
 * <{{pascalCase name}}>Content</{{pascalCase name}}>
 * ```
 */
export const {{pascalCase name}} = React.forwardRef<HTMLDivElement, {{pascalCase name}}Props>(
  ({ className, variant, children, ...props }, ref) => {
    return (
      <div
        ref={ref}
        className={cn({{camelCase name}}Variants({ variant }), className)}
        {...props}
      >
        {children}
      </div>
    );
  }
);

{{pascalCase name}}.displayName = '{{pascalCase name}}';
